apiVersion: v1
kind: Secret
metadata:
  name: signal-agent
  namespace: beagle-access
type: Opaque
stringData:
  # Agent 认证 Token
  # 1. 在 Server Web 界面创建 Agent
  # 2. 复制生成的 token
  # 3. 替换下面的 "your-agent-token-here"
  token: "your-agent-token-here"
