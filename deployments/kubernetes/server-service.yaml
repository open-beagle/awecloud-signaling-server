apiVersion: v1
kind: Service
metadata:
  name: awecloud-signaling-server
  namespace: awecloud
  labels:
    app: awecloud-signaling-server
spec:
  type: ClusterIP
  ports:
  - name: frp
    port: 7000
    targetPort: 7000
    protocol: TCP
  - name: web
    port: 8080
    targetPort: 8080
    protocol: TCP
  - name: grpc
    port: 8081
    targetPort: 8081
    protocol: TCP
  selector:
    app: awecloud-signaling-server
