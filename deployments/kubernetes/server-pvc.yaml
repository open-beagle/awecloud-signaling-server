apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: signal-server
  namespace: beagle-access
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
