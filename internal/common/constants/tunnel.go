package constants

// 隧道相关常量
const (
	// DefaultWebSocketPath 默认的 WebSocket 路径
	// 使用 /ws 而不是 FRP 默认的 /~!frp
	DefaultWebSocketPath = "/ws"

	// FRPDefaultPath FRP 原生的默认路径（用于兼容）
	FRPDefaultPath = "/~!frp"
)
